# Lean dependency set for CI — excludes torch/demucs (huge, not needed:
# benchmark fixtures ship pre-rendered drums stems).
numpy>=2.5.1
scipy>=1.18.0
librosa>=0.11.0
soundfile>=0.14.0
pyrubberband>=0.4.0
requests>=2.34.2
pytest>=9.1.1
pytest-cov>=7.1.0
